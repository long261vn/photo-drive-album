# Tham chiếu Flickr

Nguồn tham chiếu: <https://flickr.com/photos/199616417@N03/albums/72177720331444706/>

Album Flickr mẫu là `CN Thường Niên A`, hiển thị 52 ảnh và cung cấp ba chế độ ảnh ở thanh thao tác: **Justified view**, **Standard view** và **Large view**, bên cạnh slideshow, tải xuống và chia sẻ.

Những hành vi cần đưa vào Thư viện Phụng vụ gồm: ảnh luôn giữ nguyên tỷ lệ khung (không dùng crop `object-fit: cover`), chế độ ảnh lớn tập trung vào một ảnh mỗi hàng, lưới ảnh nhỏ nhiều cột để duyệt nhanh, và danh sách có thumbnail nguyên tỷ lệ đi cùng tên tệp, ngày, loại tệp và kích thước/định dạng nếu có. Mỗi ảnh có thể mở lightbox và tải trực tiếp.

Flickr đặt metadata album ở đầu trang, cho phép người xem chuyển chế độ ngay trước danh sách ảnh và dùng tên tệp làm nhãn nhận diện. Website mới sẽ dùng các khái niệm này nhưng giữ ngôn ngữ thị giác phụng vụ đã chọn.
